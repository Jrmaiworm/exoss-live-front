/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['exoss.imo.net', 'www.seti.org', 'www.imo.net'], // Adicione os domínios necessários aqui
    },
  };
  
  export default nextConfig;
  